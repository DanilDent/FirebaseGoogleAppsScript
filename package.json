{
  "name": "firebase-gas",
  "version": "23.0.0",
  "description": "Firebase API for an Apps Script Project",
  "main": "src/Code.js",
  "types": "type.d.ts",
  "scripts": {
    "test": "test",
    "status": "clasp status",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "build": "node build/compile.js",
    "preversion": " ",
    "postversion": "echo $npm_package_version",
    "preinstall": "npm config set foo bar",
    "postinstall": "npm config get foo",
    "verb": "node build/version.js $TAG_NAME \"$(git tag -l --format='%(contents)' $TAG_NAME)\"",
    "echo-v": "echo $npm_package_version"
  },
  "author": "Kelly Ferrone",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^2.3.0",
    "@types/google-apps-script": "0.0.31",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  }
}
